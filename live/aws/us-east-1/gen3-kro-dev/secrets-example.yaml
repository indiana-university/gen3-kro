csoc:
  alias: "gen3-csoc-dev"
  cluster_name: "gen3-csoc-dev"
  provider:
    name: "aws"
    aws_profile: "boadeyem_tf"
    region: "us-east-1"
    terraform_state_bucket: ""

    # Azure
    # subscription_id: "your-subscription-id-here"
    # tenant_id: "your-tenant-id-here"
    # location: "eastus"
    # terraform_state_storage_account: "gen3kroterraformstate"
    # terraform_state_container: "tfstate"

    # GCP
    # project_id: "your-project-id-here"
    # region: "us-central1"
    # credentials_file: "/credentials/csoc.json"
    # terraform_state_bucket: "gen3-kro-terraform-state"

  vpc:
    enable_vpc: true
    vpc_name: "gen3-kro-vpc"
    vpc_cidr: "10.0.0.0/16"
    enable_nat_gateway: true
    single_nat_gateway: true
    # Explicit subnet configuration (5 AZs in us-east-1)
    availability_zones:
      - "us-east-1a"
      - "us-east-1b"
      # - "us-east-1c"
      # - "us-east-1d"
      # - "us-east-1f"
    # Private subnets: /20 blocks (4096 IPs each) for EKS nodes
    private_subnet_cidrs:
      - "10.0.0.0/20" # us-east-1a: 10.0.0.0 - 10.0.15.255
      - "10.0.16.0/20" # us-east-1b: 10.0.16.0 - 10.0.31.255
      # - "10.0.32.0/20" # us-east-1c: 10.0.32.0 - 10.0.47.255
      # - "10.0.48.0/20" # us-east-1d: 10.0.48.0 - 10.0.63.255
      # - "10.0.64.0/20" # us-east-1f: 10.0.64.0 - 10.0.79.255
    # Public subnets: /24 blocks (256 IPs each) for load balancers
    public_subnet_cidrs:
      - "10.0.240.0/24" # us-east-1a: 10.0.240.0 - 10.0.240.255
      # - "10.0.241.0/24" # us-east-1b: 10.0.241.0 - 10.0.241.255
      # - "10.0.242.0/24" # us-east-1c: 10.0.242.0 - 10.0.242.255
      # - "10.0.243.0/24" # us-east-1d: 10.0.243.0 - 10.0.243.255
      # - "10.0.244.0/24" # us-east-1f: 10.0.244.0 - 10.0.244.255
    public_subnet_tags: {}
    private_subnet_tags: {}

  k8s_cluster:
    enable_cluster: true
    kubernetes_version: "1.33"
    cluster_endpoint_public_access: true
    enable_cluster_creator_admin_permissions: true
    cluster_tags: {}
    cluster_compute_config:
      enabled: true
      node_pools: ["general-purpose"]

  gitops:
    org_name: "indiana-university"
    repo_name: "gen3-kro"
    github_url: "github.com"
    branch: "test-kro-graphs"
    bootstrap_path: "argocd/bootstrap"

  addon_configs:
    argocd:
      enable_argocd: true
    external-secrets:
      enable_identity: true
      # override_id: "arn:aws:iam::123456789012:role/existing-external-secrets-role"  # Optional: use existing IAM role

  ack_configs:
    ack-cloudtrail:
      enable_identity: true
      # override_id: "arn:aws:iam::123456789012:role/existing-cloudtrail-role"  # Optional: use existing IAM role
    ack-cloudwatchlogs:
      enable_identity: true
      # override_id: "arn:aws:iam::123456789012:role/existing-cloudwatchlogs-role"  # Optional: use existing IAM role
    ack-ec2:
      enable_identity: true
      # override_id: "arn:aws:iam::123456789012:role/existing-ec2-role"  # Optional: use existing IAM role
    ack-efs:
      enable_identity: true
      # override_id: "arn:aws:iam::123456789012:role/existing-efs-role"  # Optional: use existing IAM role
    ack-eks:
      enable_identity: true
      # override_id: "arn:aws:iam::123456789012:role/existing-eks-role"  # Optional: use existing IAM role
    ack-elasticache:
      enable_identity: true
      # override_id: "arn:aws:iam::123456789012:role/existing-elasticache-role"  # Optional: use existing IAM role
    ack-iam:
      enable_identity: true
      # override_id: "arn:aws:iam::123456789012:role/existing-iam-role"  # Optional: use existing IAM role
    ack-kms:
      enable_identity: true
      # override_id: "arn:aws:iam::123456789012:role/existing-kms-role"  # Optional: use existing IAM role
    ack-opensearchservice:
      enable_identity: true
      # override_id: "arn:aws:iam::123456789012:role/existing-opensearchservice-role"  # Optional: use existing IAM role
    ack-rds:
      enable_identity: true
      # override_id: "arn:aws:iam::123456789012:role/existing-rds-role"  # Optional: use existing IAM role
    ack-route53:
      enable_identity: true
      # override_id: "arn:aws:iam::123456789012:role/existing-route53-role"  # Optional: use existing IAM role
    ack-s3:
      enable_identity: true
      # override_id: "arn:aws:iam::123456789012:role/existing-s3-role"  # Optional: use existing IAM role
    ack-secretsmanager:
      enable_identity: true
      # override_id: "arn:aws:iam::123456789012:role/existing-secretsmanager-role"  # Optional: use existing IAM role
    ack-sns:
      enable_identity: true
      # override_id: "arn:aws:iam::123456789012:role/existing-sns-role"  # Optional: use existing IAM role
    ack-sqs:
      enable_identity: true
      # override_id: "arn:aws:iam::123456789012:role/existing-sqs-role"  # Optional: use existing IAM role
    ack-wafv2:
      enable_identity: true
      # override_id: "arn:aws:iam::123456789012:role/existing-wafv2-role"  # Optional: use existing IAM role

  aso_configs:
    aso-monitor:
      enable_identity: true
      # override_id: "/subscriptions/12345678-1234-1234-1234-123456789012/resourceGroups/rg-name/providers/Microsoft.ManagedIdentity/userAssignedIdentities/existing-monitor-identity"  # Optional: use existing managed identity
    aso-compute:
      enable_identity: true
      # override_id: "/subscriptions/12345678-1234-1234-1234-123456789012/resourceGroups/rg-name/providers/Microsoft.ManagedIdentity/userAssignedIdentities/existing-compute-identity"  # Optional: use existing managed identity
    aso-storage:
      enable_identity: true
      # override_id: "/subscriptions/12345678-1234-1234-1234-123456789012/resourceGroups/rg-name/providers/Microsoft.ManagedIdentity/userAssignedIdentities/existing-storage-identity"  # Optional: use existing managed identity
    aso-aks:
      enable_identity: true
      # override_id: "/subscriptions/12345678-1234-1234-1234-123456789012/resourceGroups/rg-name/providers/Microsoft.ManagedIdentity/userAssignedIdentities/existing-aks-identity"  # Optional: use existing managed identity
    aso-sql:
      enable_identity: true
      # override_id: "/subscriptions/12345678-1234-1234-1234-123456789012/resourceGroups/rg-name/providers/Microsoft.ManagedIdentity/userAssignedIdentities/existing-sql-identity"  # Optional: use existing managed identity
    aso-dns:
      enable_identity: true
      # override_id: "/subscriptions/12345678-1234-1234-1234-123456789012/resourceGroups/rg-name/providers/Microsoft.ManagedIdentity/userAssignedIdentities/existing-dns-identity"  # Optional: use existing managed identity
    aso-keyvault:
      enable_identity: true
      # override_id: "/subscriptions/12345678-1234-1234-1234-123456789012/resourceGroups/rg-name/providers/Microsoft.ManagedIdentity/userAssignedIdentities/existing-keyvault-identity"  # Optional: use existing managed identity
    aso-servicebus:
      enable_identity: true
      # override_id: "/subscriptions/12345678-1234-1234-1234-123456789012/resourceGroups/rg-name/providers/Microsoft.ManagedIdentity/userAssignedIdentities/existing-servicebus-identity"  # Optional: use existing managed identity

  gcc_configs:
    gcc-logging:
      enable_identity: true
      # override_id: "existing-logging-sa@project-id.iam.gserviceaccount.com"  # Optional: use existing service account
    gcc-monitoring:
      enable_identity: true
      # override_id: "existing-monitoring-sa@project-id.iam.gserviceaccount.com"  # Optional: use existing service account
    gcc-compute:
      enable_identity: true
      # override_id: "existing-compute-sa@project-id.iam.gserviceaccount.com"  # Optional: use existing service account
    gcc-storage:
      enable_identity: true
      # override_id: "existing-storage-sa@project-id.iam.gserviceaccount.com"  # Optional: use existing service account
    gcc-gke:
      enable_identity: true
      # override_id: "existing-gke-sa@project-id.iam.gserviceaccount.com"  # Optional: use existing service account
    gcc-sql:
      enable_identity: true
      # override_id: "existing-sql-sa@project-id.iam.gserviceaccount.com"  # Optional: use existing service account
    gcc-dns:
      enable_identity: true
      # override_id: "existing-dns-sa@project-id.iam.gserviceaccount.com"  # Optional: use existing service account
    gcc-kms:
      enable_identity: true
      # override_id: "existing-kms-sa@project-id.iam.gserviceaccount.com"  # Optional: use existing service account
    gcc-pubsub:
      enable_identity: true
      # override_id: "existing-pubsub-sa@project-id.iam.gserviceaccount.com"  # Optional: use existing service account
    gcc-iam:
      enable_identity: true
      # override_id: "existing-iam-sa@project-id.iam.gserviceaccount.com"  # Optional: use existing service account
    gcc-filestore:
      enable_identity: true
      # override_id: "existing-filestore-sa@project-id.iam.gserviceaccount.com"  # Optional: use existing service account


# Base tags applied to all resources
tags:
  ManagedBy: "terragrunt"
  Owner: "RDS Team"

paths:
  outputs_dir: "outputs/terraform"
  iam_base_path: "iam"

# Spoke GitOps configuration (used as defaults for all spokes)
spokes_gitops:
  org_name: "indiana-university"
  repo_name: "gen3-kro"
  github_url: "github.com"
  branch: "test-kro-graphs"
  path: "argocd/deployments/gen3-spokes"

spokes:
  - alias: "spoke1"
    enabled: true
    provider:
      name: "aws"
      aws_profile: "boadeyem_tf"
      region: "us-east-1"
      # gcp_project_id: "your-project-id-here"
      # gcp_region: "us-central1"
      # azure_subscription_id: "your-subscription-id-here"
      # azure_location: "eastus"

    addon_configs:
      # GitOps & Application Management
      argocd:
        enable_identity: true  # enable ArgoCD pod identity
        # override_id: "arn:aws:iam::123456789012:role/existing-argocd-role"  # Optional: use existing IAM role
      external-secrets:
        enable_identity: true  # enable External Secrets pod identity
        # override_id: "arn:aws:iam::123456789012:role/existing-external-secrets-role"  # Optional: use existing IAM role

    ack_configs:
      # ACK Controllers
      ack-cloudtrail:
        enable_identity: true  # enable CloudTrail controller pod identity
        # override_id: "arn:aws:iam::123456789012:role/existing-cloudtrail-role"  # Optional: use existing IAM role
      ack-cloudwatchlogs:
        enable_identity: true  # enable CloudWatch Logs controller pod identity
        # override_id: "arn:aws:iam::123456789012:role/existing-cloudwatchlogs-role"  # Optional: use existing IAM role
      ack-ec2:
        enable_identity: true  # enable EC2 controller pod identity
        # override_id: "arn:aws:iam::123456789012:role/existing-ec2-role"  # Optional: use existing IAM role
      ack-efs:
        enable_identity: true  # enable EFS controller pod identity
        # override_id: "arn:aws:iam::123456789012:role/existing-efs-role"  # Optional: use existing IAM role
      ack-eks:
        enable_identity: true  # enable EKS controller pod identity
        # override_id: "arn:aws:iam::123456789012:role/existing-eks-role"  # Optional: use existing IAM role
      ack-elasticache:
        enable_identity: true  # enable ElastiCache controller pod identity
        # override_id: "arn:aws:iam::123456789012:role/existing-elasticache-role"  # Optional: use existing IAM role
      ack-iam:
        enable_identity: true  # enable IAM controller pod identity
        # override_id: "arn:aws:iam::123456789012:role/existing-iam-role"  # Optional: use existing IAM role
      ack-kms:
        enable_identity: true  # enable KMS controller pod identity
        # override_id: "arn:aws:iam::123456789012:role/existing-kms-role"  # Optional: use existing IAM role
      ack-opensearchservice:
        enable_identity: true  # enable OpenSearch controller pod identity
        # override_id: "arn:aws:iam::123456789012:role/existing-opensearchservice-role"  # Optional: use existing IAM role
      ack-rds:
        enable_identity: true  # enable RDS controller pod identity
        # override_id: "arn:aws:iam::123456789012:role/existing-rds-role"  # Optional: use existing IAM role
      ack-route53:
        enable_identity: true  # enable Route53 controller pod identity
        # override_id: "arn:aws:iam::123456789012:role/existing-route53-role"  # Optional: use existing IAM role
      ack-s3:
        enable_identity: true  # enable S3 controller pod identity
        # override_id: "arn:aws:iam::123456789012:role/existing-s3-role"  # Optional: use existing IAM role
      ack-secretsmanager:
        enable_identity: true  # enable Secrets Manager controller pod identity
        # override_id: "arn:aws:iam::123456789012:role/existing-secretsmanager-role"  # Optional: use existing IAM role
      ack-sns:
        enable_identity: true  # enable SNS controller pod identity
        # override_id: "arn:aws:iam::123456789012:role/existing-sns-role"  # Optional: use existing IAM role
      ack-sqs:
        enable_identity: true  # enable SQS controller pod identity
        # override_id: "arn:aws:iam::123456789012:role/existing-sqs-role"  # Optional: use existing IAM role
      ack-wafv2:
        enable_identity: true  # enable WAFv2 controller pod identity
        # override_id: "arn:aws:iam::123456789012:role/existing-wafv2-role"  # Optional: use existing IAM role

    aso_configs:
      aso-monitor:
        enable_identity: true  # enable Azure Monitor controller workload identity
        # override_id: "/subscriptions/12345678-1234-1234-1234-123456789012/resourceGroups/rg-name/providers/Microsoft.ManagedIdentity/userAssignedIdentities/existing-monitor-identity"  # Optional: use existing managed identity
      aso-compute:
        enable_identity: true  # enable Azure Compute controller workload identity
        # override_id: "/subscriptions/12345678-1234-1234-1234-123456789012/resourceGroups/rg-name/providers/Microsoft.ManagedIdentity/userAssignedIdentities/existing-compute-identity"  # Optional: use existing managed identity
      aso-storage:
        enable_identity: true  # enable Azure Storage controller workload identity
        # override_id: "/subscriptions/12345678-1234-1234-1234-123456789012/resourceGroups/rg-name/providers/Microsoft.ManagedIdentity/userAssignedIdentities/existing-storage-identity"  # Optional: use existing managed identity
      aso-aks:
        enable_identity: true  # enable Azure AKS controller workload identity
        # override_id: "/subscriptions/12345678-1234-1234-1234-123456789012/resourceGroups/rg-name/providers/Microsoft.ManagedIdentity/userAssignedIdentities/existing-aks-identity"  # Optional: use existing managed identity
      aso-sql:
        enable_identity: true  # enable Azure SQL controller workload identity
        # override_id: "/subscriptions/12345678-1234-1234-1234-123456789012/resourceGroups/rg-name/providers/Microsoft.ManagedIdentity/userAssignedIdentities/existing-sql-identity"  # Optional: use existing managed identity
      aso-dns:
        enable_identity: true  # enable Azure DNS controller workload identity
        # override_id: "/subscriptions/12345678-1234-1234-1234-123456789012/resourceGroups/rg-name/providers/Microsoft.ManagedIdentity/userAssignedIdentities/existing-dns-identity"  # Optional: use existing managed identity
      aso-keyvault:
        enable_identity: true  # enable Azure KeyVault controller workload identity
        # override_id: "/subscriptions/12345678-1234-1234-1234-123456789012/resourceGroups/rg-name/providers/Microsoft.ManagedIdentity/userAssignedIdentities/existing-keyvault-identity"  # Optional: use existing managed identity
      aso-servicebus:
        enable_identity: true  # enable Azure Service Bus controller workload identity
        # override_id: "/subscriptions/12345678-1234-1234-1234-123456789012/resourceGroups/rg-name/providers/Microsoft.ManagedIdentity/userAssignedIdentities/existing-servicebus-identity"  # Optional: use existing managed identity

    gcc_configs:
      gcc-logging:
        enable_identity: true  # enable GCP Logging controller workload identity
        # override_id: "existing-logging-sa@project-id.iam.gserviceaccount.com"  # Optional: use existing service account
      gcc-monitoring:
        enable_identity: true  # enable GCP Monitoring controller workload identity
        # override_id: "existing-monitoring-sa@project-id.iam.gserviceaccount.com"  # Optional: use existing service account
      gcc-compute:
        enable_identity: true  # enable GCP Compute controller workload identity
        # override_id: "existing-compute-sa@project-id.iam.gserviceaccount.com"  # Optional: use existing service account
      gcc-storage:
        enable_identity: true  # enable GCP Storage controller workload identity
        # override_id: "existing-storage-sa@project-id.iam.gserviceaccount.com"  # Optional: use existing service account
      gcc-gke:
        enable_identity: true  # enable GCP GKE controller workload identity
        # override_id: "existing-gke-sa@project-id.iam.gserviceaccount.com"  # Optional: use existing service account
      gcc-sql:
        enable_identity: true  # enable GCP SQL controller workload identity
        # override_id: "existing-sql-sa@project-id.iam.gserviceaccount.com"  # Optional: use existing service account
      gcc-dns:
        enable_identity: true  # enable GCP DNS controller workload identity
        # override_id: "existing-dns-sa@project-id.iam.gserviceaccount.com"  # Optional: use existing service account
      gcc-kms:
        enable_identity: true  # enable GCP KMS controller workload identity
        # override_id: "existing-kms-sa@project-id.iam.gserviceaccount.com"  # Optional: use existing service account
      gcc-pubsub:
        enable_identity: true  # enable GCP Pub/Sub controller workload identity
        # override_id: "existing-pubsub-sa@project-id.iam.gserviceaccount.com"  # Optional: use existing service account
      gcc-iam:
        enable_identity: true  # enable GCP IAM controller workload identity
        # override_id: "existing-iam-sa@project-id.iam.gserviceaccount.com"  # Optional: use existing service account
      gcc-filestore:
        enable_identity: true  # enable GCP Filestore controller workload identity
        # override_id: "existing-filestore-sa@project-id.iam.gserviceaccount.com"  # Optional: use existing service account

    gitops:
      org_name: "indiana-university"
      repo_name: "gen3-kro"
      github_url: "github.com"
      branch: "test-kro-graphs"
      repo_url: "https://github.com/indiana-university/gen3-kro.git"
      argo_path: "argocd/spokes"

    tags:
      Team: "TestingSpoke"
      Purpose: "same-account-demo"

  - alias: "spoke2"
    enabled: true
    region: "us-east-1"
    provider:
      name: "aws"
      aws_profile: "jayadeyemi"
      region: "us-east-1"
      # gcp_project_id: "your-project-id-here"
      # gcp_region: "us-central1"
      # azure_subscription_id: "your-subscription-id-here"
      # azure_location: "eastus"

    addon_configs:
      # GitOps & Application Management
      argocd:
        enable_identity: true  # enable ArgoCD pod identity
        # override_id: "arn:aws:iam::987654321098:role/existing-argocd-role"  # Optional: use existing IAM role
      # Secrets Management
      external-secrets:
        enable_identity: true  # enable External Secrets pod identity
        # override_id: "arn:aws:iam::987654321098:role/existing-external-secrets-role"  # Optional: use existing IAM role

    ack_configs:
      # ACK Controllers
      ack-cloudtrail:
        enable_identity: true  # enable CloudTrail controller pod identity
        # override_id: "arn:aws:iam::987654321098:role/existing-cloudtrail-role"  # Optional: use existing IAM role
      ack-cloudwatchlogs:
        enable_identity: true  # enable CloudWatch Logs controller pod identity
        # override_id: "arn:aws:iam::987654321098:role/existing-cloudwatchlogs-role"  # Optional: use existing IAM role
      ack-ec2:
        enable_identity: true  # enable EC2 controller pod identity
        # override_id: "arn:aws:iam::987654321098:role/existing-ec2-role"  # Optional: use existing IAM role
      ack-efs:
        enable_identity: true  # enable EFS controller pod identity
        # override_id: "arn:aws:iam::987654321098:role/existing-efs-role"  # Optional: use existing IAM role
      ack-eks:
        enable_identity: true  # enable EKS controller pod identity
        # override_id: "arn:aws:iam::987654321098:role/existing-eks-role"  # Optional: use existing IAM role
      ack-elasticache:
        enable_identity: true  # enable ElastiCache controller pod identity
        # override_id: "arn:aws:iam::987654321098:role/existing-elasticache-role"  # Optional: use existing IAM role
      ack-iam:
        enable_identity: true  # enable IAM controller pod identity
        # override_id: "arn:aws:iam::987654321098:role/existing-iam-role"  # Optional: use existing IAM role
      ack-kms:
        enable_identity: true  # enable KMS controller pod identity
        # override_id: "arn:aws:iam::987654321098:role/existing-kms-role"  # Optional: use existing IAM role
      ack-opensearchservice:
        enable_identity: true  # enable OpenSearch controller pod identity
        # override_id: "arn:aws:iam::987654321098:role/existing-opensearchservice-role"  # Optional: use existing IAM role
      ack-rds:
        enable_identity: true  # enable RDS controller pod identity
        # override_id: "arn:aws:iam::987654321098:role/existing-rds-role"  # Optional: use existing IAM role
      ack-route53:
        enable_identity: true  # enable Route53 controller pod identity
        # override_id: "arn:aws:iam::987654321098:role/existing-route53-role"  # Optional: use existing IAM role
      ack-s3:
        enable_identity: true  # enable S3 controller pod identity
        # override_id: "arn:aws:iam::987654321098:role/existing-s3-role"  # Optional: use existing IAM role
      ack-secretsmanager:
        enable_identity: true  # enable Secrets Manager controller pod identity
        # override_id: "arn:aws:iam::987654321098:role/existing-secretsmanager-role"  # Optional: use existing IAM role
      ack-sns:
        enable_identity: true  # enable SNS controller pod identity
        # override_id: "arn:aws:iam::987654321098:role/existing-sns-role"  # Optional: use existing IAM role
      ack-sqs:
        enable_identity: true  # enable SQS controller pod identity
        # override_id: "arn:aws:iam::987654321098:role/existing-sqs-role"  # Optional: use existing IAM role
      ack-wafv2:
        enable_identity: true  # enable WAFv2 controller pod identity
        # override_id: "arn:aws:iam::987654321098:role/existing-wafv2-role"  # Optional: use existing IAM role

    aso_configs:
      aso-monitor:
        enable_identity: true  # enable Azure Monitor controller workload identity
        # override_id: "/subscriptions/98765432-9876-9876-9876-987654321098/resourceGroups/rg-name/providers/Microsoft.ManagedIdentity/userAssignedIdentities/existing-monitor-identity"  # Optional: use existing managed identity
      aso-compute:
        enable_identity: true  # enable Azure Compute controller workload identity
        # override_id: "/subscriptions/98765432-9876-9876-9876-987654321098/resourceGroups/rg-name/providers/Microsoft.ManagedIdentity/userAssignedIdentities/existing-compute-identity"  # Optional: use existing managed identity
      aso-storage:
        enable_identity: true  # enable Azure Storage controller workload identity
        # override_id: "/subscriptions/98765432-9876-9876-9876-987654321098/resourceGroups/rg-name/providers/Microsoft.ManagedIdentity/userAssignedIdentities/existing-storage-identity"  # Optional: use existing managed identity
      aso-aks:
        enable_identity: true  # enable Azure AKS controller workload identity
        # override_id: "/subscriptions/98765432-9876-9876-9876-987654321098/resourceGroups/rg-name/providers/Microsoft.ManagedIdentity/userAssignedIdentities/existing-aks-identity"  # Optional: use existing managed identity
      aso-sql:
        enable_identity: true  # enable Azure SQL controller workload identity
        # override_id: "/subscriptions/98765432-9876-9876-9876-987654321098/resourceGroups/rg-name/providers/Microsoft.ManagedIdentity/userAssignedIdentities/existing-sql-identity"  # Optional: use existing managed identity
      aso-dns:
        enable_identity: true  # enable Azure DNS controller workload identity
        # override_id: "/subscriptions/98765432-9876-9876-9876-987654321098/resourceGroups/rg-name/providers/Microsoft.ManagedIdentity/userAssignedIdentities/existing-dns-identity"  # Optional: use existing managed identity
      aso-keyvault:
        enable_identity: true  # enable Azure KeyVault controller workload identity
        # override_id: "/subscriptions/98765432-9876-9876-9876-987654321098/resourceGroups/rg-name/providers/Microsoft.ManagedIdentity/userAssignedIdentities/existing-keyvault-identity"  # Optional: use existing managed identity
      aso-servicebus:
        enable_identity: true  # enable Azure Service Bus controller workload identity
        # override_id: "/subscriptions/98765432-9876-9876-9876-987654321098/resourceGroups/rg-name/providers/Microsoft.ManagedIdentity/userAssignedIdentities/existing-servicebus-identity"  # Optional: use existing managed identity

    gcc_configs:
      gcc-logging:
        enable_identity: true  # enable GCP Logging controller workload identity
        # override_id: "existing-logging-sa@project-id-2.iam.gserviceaccount.com"  # Optional: use existing service account
      gcc-monitoring:
        enable_identity: true  # enable GCP Monitoring controller workload identity
        # override_id: "existing-monitoring-sa@project-id-2.iam.gserviceaccount.com"  # Optional: use existing service account
      gcc-compute:
        enable_identity: true  # enable GCP Compute controller workload identity
        # override_id: "existing-compute-sa@project-id-2.iam.gserviceaccount.com"  # Optional: use existing service account
      gcc-storage:
        enable_identity: true  # enable GCP Storage controller workload identity
        # override_id: "existing-storage-sa@project-id-2.iam.gserviceaccount.com"  # Optional: use existing service account
      gcc-gke:
        enable_identity: true  # enable GCP GKE controller workload identity
        # override_id: "existing-gke-sa@project-id-2.iam.gserviceaccount.com"  # Optional: use existing service account
      gcc-sql:
        enable_identity: true  # enable GCP SQL controller workload identity
        # override_id: "existing-sql-sa@project-id-2.iam.gserviceaccount.com"  # Optional: use existing service account
      gcc-dns:
        enable_identity: true  # enable GCP DNS controller workload identity
        # override_id: "existing-dns-sa@project-id-2.iam.gserviceaccount.com"  # Optional: use existing service account
      gcc-kms:
        enable_identity: true  # enable GCP KMS controller workload identity
        # override_id: "existing-kms-sa@project-id-2.iam.gserviceaccount.com"  # Optional: use existing service account
      gcc-pubsub:
        enable_identity: true  # enable GCP Pub/Sub controller workload identity
        # override_id: "existing-pubsub-sa@project-id-2.iam.gserviceaccount.com"  # Optional: use existing service account
      gcc-iam:
        enable_identity: true  # enable GCP IAM controller workload identity
        # override_id: "existing-iam-sa@project-id-2.iam.gserviceaccount.com"  # Optional: use existing service account
      gcc-filestore:
        enable_identity: true  # enable GCP Filestore controller workload identity
        # override_id: "existing-filestore-sa@project-id-2.iam.gserviceaccount.com"  # Optional: use existing service account

    gitops:
      org_name: "indiana-university"
      repo_name: "gen3-kro"
      github_url: "github.com"
      branch: "test-kro-graphs"
      repo_url: "https://github.com/indiana-university/gen3-kro.git"
      argo_path: "argocd/spokes"

    tags:
      Team: "TestingSpoke2"
      Purpose: "duplicate-spoke-test"
