# spoke2 fleet

Placeholder directory — no infrastructure instances deployed yet.

The `spoke2-fleet-instances` ArgoCD Application expects this directory to exist
(resolved from `argocd/fleet/spoke2` via the default `fleet_instances_path` logic).
Add `infrastructure/instances.yaml` and `infrastructure/infrastucture-values.yaml`
here once spoke2 resources are ready to deploy.

See `argocd/fleet/spoke1/` for the spoke1 reference layout.
