apiVersion: kro.run/v1alpha1
kind: ResourceGraphDefinition
metadata:
  name: awssgaurorat1
spec:
  schema:
    apiVersion: v1alpha1
    kind: AwsSgAuroraT1
    spec:
      name: string
      namespace: string | default="default"
      region: string | default="us-east-1"
      environment: string | default="dev"
      project: string | default="kro-managed"
      vpcID: string
      eksNodeSecurityGroupID: string
      port: integer | default=5432
      adoptionPolicy: string | default="adopt-or-create"
      deletionPolicy: string | default="retain"
    status:
      securityGroupID: "${auroraSG.status.?id.orValue('')}"
      emptySecurityGroupID: "${auroraEmptySG.status.?id.orValue('')}"
      conditions: "${auroraSG.status.?conditions.orValue([])}"
      ready: "${auroraSG.status.?id.orValue('') != '' && auroraEmptySG.status.?id.orValue('') != ''}"
  resources:
  - id: auroraEmptySG
    readyWhen:
      - ${auroraEmptySG.status.conditions.exists(c, c.type == "ACK.ResourceSynced" && c.status == "True")}
      - ${auroraEmptySG.status.id != ""}
    template:
      apiVersion: ec2.services.k8s.aws/v1alpha1
      kind: SecurityGroup
      metadata:
        name: ${schema.spec.name}-aurora-empty-sg
        namespace: ${schema.spec.namespace}
        annotations:
          services.k8s.aws/region: ${schema.spec.region}
          services.k8s.aws/adoption-policy: ${schema.spec.adoptionPolicy}
          services.k8s.aws/deletion-policy: ${schema.spec.deletionPolicy}
      spec:
        name: ${schema.spec.name}-aurora-empty-sg
        description: Empty security group for Aurora PostgreSQL (no rules)
        vpcID: ${schema.spec.vpcID}
        tags:
          - key: Name
            value: ${schema.spec.name}-aurora-empty-sg
          - key: Environment
            value: ${schema.spec.environment}
          - key: ManagedBy
            value: Kro
          - key: Project
            value: ${schema.spec.project}
          - key: Purpose
            value: Aurora-Empty
  - id: auroraSG
    readyWhen:
      - ${auroraSG.status.conditions.exists(c, c.type == "ACK.ResourceSynced" && c.status == "True")}
      - ${auroraSG.status.id != ""}
    template:
      apiVersion: ec2.services.k8s.aws/v1alpha1
      kind: SecurityGroup
      metadata:
        name: ${schema.spec.name}-aurora-sg
        namespace: ${schema.spec.namespace}
        annotations:
          services.k8s.aws/region: ${schema.spec.region}
          services.k8s.aws/adoption-policy: ${schema.spec.adoptionPolicy}
          services.k8s.aws/deletion-policy: ${schema.spec.deletionPolicy}
      spec:
        name: ${schema.spec.name}-aurora-sg
        description: Security group for Aurora PostgreSQL database
        vpcID: ${schema.spec.vpcID}
        ingressRules:
          - ipProtocol: tcp
            fromPort: ${schema.spec.port}
            toPort: ${schema.spec.port}
            userIDGroupPairs:
              - groupID: ${schema.spec.eksNodeSecurityGroupID}
                description: PostgreSQL from EKS nodes
        egressRules:
          - ipProtocol: "-1"
            ipRanges:
              - cidrIP: 0.0.0.0/0
                description: Allow all outbound
        tags:
          - key: Name
            value: ${schema.spec.name}-aurora-sg
          - key: Environment
            value: ${schema.spec.environment}
          - key: ManagedBy
            value: Kro
          - key: Project
            value: ${schema.spec.project}
          - key: Purpose
            value: Aurora-PostgreSQL
