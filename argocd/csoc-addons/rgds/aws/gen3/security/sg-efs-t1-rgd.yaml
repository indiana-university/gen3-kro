apiVersion: kro.run/v1alpha1
kind: ResourceGraphDefinition
metadata:
  name: awssgefst1
spec:
  schema:
    apiVersion: v1alpha1
    kind: AwsSgEfsT1
    spec:
      name: string
      namespace: string | default="default"
      region: string | default="us-east-1"
      environment: string | default="dev"
      project: string | default="kro-managed"
      vpcID: string
      eksNodeSecurityGroupID: string
      port: integer | default=2049
      adoptionPolicy: string | default="adopt-or-create"
      deletionPolicy: string | default="retain"
    status:
      securityGroupID: "${efsSG.status.?id.orValue('')}"
      emptySecurityGroupID: "${efsEmptySG.status.?id.orValue('')}"
      conditions: "${efsSG.status.?conditions.orValue([])}"
      ready: "${efsSG.status.?id.orValue('') != '' && efsEmptySG.status.?id.orValue('') != ''}"
  resources:
  - id: efsEmptySG
    readyWhen:
      - ${efsEmptySG.status.conditions.exists(c, c.type == "ACK.ResourceSynced" && c.status == "True")}
      - ${efsEmptySG.status.id != ""}
    template:
      apiVersion: ec2.services.k8s.aws/v1alpha1
      kind: SecurityGroup
      metadata:
        name: ${schema.spec.name}-efs-empty-sg
        namespace: ${schema.spec.namespace}
        annotations:
          services.k8s.aws/region: ${schema.spec.region}
          services.k8s.aws/adoption-policy: ${schema.spec.adoptionPolicy}
          services.k8s.aws/deletion-policy: ${schema.spec.deletionPolicy}
      spec:
        name: ${schema.spec.name}-efs-empty-sg
        description: Empty security group for EFS (no rules)
        vpcID: ${schema.spec.vpcID}
        tags:
          - key: Name
            value: ${schema.spec.name}-efs-empty-sg
          - key: Environment
            value: ${schema.spec.environment}
          - key: ManagedBy
            value: Kro
          - key: Project
            value: ${schema.spec.project}
          - key: Purpose
            value: EFS-Empty
  - id: efsSG
    readyWhen:
      - ${efsSG.status.conditions.exists(c, c.type == "ACK.ResourceSynced" && c.status == "True")}
      - ${efsSG.status.id != ""}
    template:
      apiVersion: ec2.services.k8s.aws/v1alpha1
      kind: SecurityGroup
      metadata:
        name: ${schema.spec.name}-efs-sg
        namespace: ${schema.spec.namespace}
        annotations:
          services.k8s.aws/region: ${schema.spec.region}
          services.k8s.aws/adoption-policy: ${schema.spec.adoptionPolicy}
          services.k8s.aws/deletion-policy: ${schema.spec.deletionPolicy}
      spec:
        name: ${schema.spec.name}-efs-sg
        description: Security group for EFS file system
        vpcID: ${schema.spec.vpcID}
        ingressRules:
          - ipProtocol: tcp
            fromPort: ${schema.spec.port}
            toPort: ${schema.spec.port}
            userIDGroupPairs:
              - groupID: ${schema.spec.eksNodeSecurityGroupID}
                description: NFS from EKS nodes
        egressRules:
          - ipProtocol: "-1"
            ipRanges:
              - cidrIP: 0.0.0.0/0
                description: Allow all outbound
        tags:
          - key: Name
            value: ${schema.spec.name}-efs-sg
          - key: Environment
            value: ${schema.spec.environment}
          - key: ManagedBy
            value: Kro
          - key: Project
            value: ${schema.spec.project}
          - key: Purpose
            value: EFS
