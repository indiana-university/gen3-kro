apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# AWS Layer-2 ResourceGraphDefinitions
# Composite/aggregated resources that depend on layer-1
# Sync wave 0: Deploy after layer-1 resources are ready

commonAnnotations:
  argocd.argoproj.io/sync-wave: "0"

# resources:
  # Gen3 Infrastructure - Composite resource (commented out - enable when layer-1 is stable)
  # - gen3-infrastructure-rgd.yaml
