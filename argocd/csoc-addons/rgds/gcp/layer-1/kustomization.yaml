apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# GCP Layer-1 ResourceGraphDefinitions
# Foundational infrastructure resources (VPC, Storage, etc.)
# Sync wave -4: Deploy before layer-2 resources

commonAnnotations:
  argocd.argoproj.io/sync-wave: "-4"

# resources:
  # VPC - Core networking infrastructure (ACTIVE)
  # - vpc-rgd.yaml

  # Additional Layer-1 RGDs (commented out - enable as needed)
  # - cloudsql-rgd.yaml
  # - gke-rgd.yaml
  # - storage-bucket-rgd.yaml
