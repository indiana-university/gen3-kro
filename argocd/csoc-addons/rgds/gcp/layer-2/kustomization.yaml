apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# GCP Layer-2 ResourceGraphDefinitions
# Composite/aggregated resources that depend on layer-1
# Sync wave 0: Deploy after layer-1 resources are ready

commonAnnotations:
  argocd.argoproj.io/sync-wave: "0"

# resources:
  # Add composite GCP RGDs here when available
