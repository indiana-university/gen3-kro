apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ack-controllers.yaml
  - other-addons.yaml
  - spoke-template/kustomization.yaml

commonLabels:
  managed-by: argocd
  component: hub-bootstrap
