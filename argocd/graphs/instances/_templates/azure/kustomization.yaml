apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Base Azure infrastructure instances
# These serve as templates for spoke-specific overlays

resources:
  - namespace.yaml
  - vnet-instance.yaml
  - aks-instance.yaml
  - postgresql-instance.yaml
  - storage-account-instance.yaml
