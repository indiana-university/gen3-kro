apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: gen3-spoke-infrastructure
  namespace: argocd-ns
  annotations:
    argocd.argoproj.io/sync-wave: "0"
spec:
  goTemplate: true
  goTemplateOptions: ["missingkey=error"]
  generators:
  - matrix:
      generators:
        # Read spokes-charter ConfigMap for spoke definitions
        - list:
            elements: |
              {{- range $spoke := (lookup "v1" "ConfigMap" "argocd" "spokes-charter").data.spokes | mustFromJson | get "spokes" }}
              - alias: {{ $spoke.alias }}
                environment: {{ $spoke.environment }}
                namespace_suffix: {{ $spoke.namespace_suffix }}
              {{- end }}
        - clusters:
            selector:
              matchLabels:
                fleet_member: spoke
  template:
    metadata:
      name: '{{ .alias }}-infrastructure'
      labels:
        app.kubernetes.io/name: spoke-infrastructure
        app.kubernetes.io/part-of: gen3
        spoke: '{{ .alias }}'
        environment: '{{ .environment }}'
      annotations:
        argocd.argoproj.io/sync-wave: "0"
    spec:
      project: default
      source:
        repoURL: '{{.metadata.annotations.csoc_repo_url}}'
        targetRevision: '{{.metadata.annotations.csoc_repo_revision}}'
        # Use spoke alias as folder name: argocd/deployments/gen3-spokes/<spoke_alias>
        path: '{{.metadata.annotations.csoc_repo_basepath}}/deployments/gen3-spokes/{{ .alias }}'
        kustomize:
          commonLabels:
            deployed-by: argocd
            cluster: '{{ .name }}'
            spoke: '{{ .alias }}'
            environment: '{{ .environment }}'
          # Target namespace: <spoke_alias>-<namespace_suffix>
          namespace: '{{ .alias }}-{{ .namespace_suffix }}'
      destination:
        server: '{{ .server }}'
        # Deploy to spoke infrastructure namespace
        namespace: '{{ .alias }}-{{ .namespace_suffix }}'
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
          - CreateNamespace=false  # Namespaces created by separate namespace ApplicationSet
          - ServerSideApply=true
          - SkipDryRunOnMissingResource=true
          - RespectIgnoreDifferences=true
          - PrunePropagationPolicy=foreground
          - PruneLast=true
        retry:
          limit: 5
          backoff:
            duration: 10s
            factor: 2
            maxDuration: 5m
      ignoreDifferences:
        - group: kro.run
          kind: ResourceGraph
          jsonPointers:
            - /status
        - group: kro.run
          kind: Vpc
          jsonPointers:
            - /status
        - group: ""
          kind: Namespace
          jqPathExpressions:
            - '.metadata.labels."kubernetes.io/metadata.name"'
