# # ── AwsGen3Search1 — Tier 2: Thin Search (~$30–200/mo) ───────────────────────
# # Reads the single networkBridge (Search KMS key, OpenSearch SG,
# # VPC ID, private subnet IDs) from Network.
# # Creates: OpenSearch Domain + searchBridge ConfigMap.
# #
# # RGD: awsgen3search1-rg.yaml  |  Kind: AwsGen3Search1
# #
# # PREREQUISITES:
# #   - Tier 0 (spoke1-network) must be Ready with searchEnabled: true
# #   - networkBridge must exist in namespace spoke1
# #   - ACK controller 'opensearchservice' must be running
# # ----------------------------------------------------------------------------
# apiVersion: kro.run/v1alpha1
# kind: AwsGen3Search1
# metadata:
#   name: search
#   namespace: spoke1
#   annotations:
#     argocd.argoproj.io/sync-wave: "20"
#     argocd.argoproj.io/sync-options: SkipDryRunOnMissingResource=true,ServerSideApply=true
# spec:
#   name: spoke1
#   namespace: spoke1
#   adoptionPolicy: adopt-or-create
#   deletionPolicy: delete
