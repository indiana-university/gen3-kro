# # ── AwsGen3Messaging1 — Tier 4: SQS Queues (~$1/mo) ──────────────────────────
# # SQS queues for audit and ssjdispatcher (both unconditional — SQS is free-tier).
# #
# # RGD: awsgen3messaging1-rg.yaml  |  Kind: AwsGen3Messaging1
# #
# # PREREQUISITES:
# #   - Namespace 'spoke1' must exist on the CSOC cluster
# #   - ACK controller 'sqs' must be running
# # ----------------------------------------------------------------------------
# apiVersion: kro.run/v1alpha1
# kind: AwsGen3Messaging1
# metadata:
#   name: spoke1-messaging
#   namespace: spoke1
#   annotations:
#     argocd.argoproj.io/sync-wave: "25"
#     argocd.argoproj.io/sync-options: SkipDryRunOnMissingResource=true,ServerSideApply=true
# spec:
#   name: spoke1
#   namespace: spoke1
#   region: us-east-1
#   environment: local
#   project: gen3-kro
#   adoptionPolicy: adopt-or-create
#   deletionPolicy: delete

#   # ── Bridge (creates messagingBridge for downstream tiers) ────────────────
#   createBridgeSecret: true
#   messagingBridgeName: spoke1-messaging-bridge
