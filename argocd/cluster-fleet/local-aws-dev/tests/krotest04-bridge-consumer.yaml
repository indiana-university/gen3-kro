# ── Test 04: bridge-consumer — KroBridgeConsumer ──────────────────────────────
# Validates: externalRef (Secret), cross-RGD data consumption.
# Reads bridge Secret produced by Test 03 via externalRef.
# Pure K8s only — zero AWS cost. Creates ConfigMaps.
#
# RGD: krotest04-bridge-consumer-rg.yaml  |  Kind: KroBridgeConsumer
#
# PREREQUISITES:
#   - Test 03 (kro-bridge-producer) must be deployed first (wave 15 < 20)
#   - Namespace matches producer (kro-test-bridge) for externalRef access
#
# Uncomment the block below to deploy.
# ----------------------------------------------------------------------------
# apiVersion: kro.run/v1alpha1
# kind: KroBridgeConsumer
# metadata:
#   name: kro-bridge-consumer
#   namespace: kro-test-bridge
#   annotations:
#     argocd.argoproj.io/sync-wave: "20"
#     argocd.argoproj.io/sync-options: SkipDryRunOnMissingResource=true,ServerSideApply=true,Prune=false
# spec:
#   name: bridge-consumer
#   namespace: kro-test-bridge
#   environment: local
#   bridgeSecretName: bridge-prod-bridge-outputs
