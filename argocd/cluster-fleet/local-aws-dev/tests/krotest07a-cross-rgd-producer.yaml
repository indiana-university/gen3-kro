# ── Test 07a: cross-RGD producer — KroTest07Producer ─────────────────────────
# Validates: Real ACK resources (VPC + base SecurityGroup) publishing status
# values (vpc_id, base_sg_id) to a bridge ConfigMap for cross-RGD consumption.
# Bridge ConfigMap name = {spec.name}-crossrgd-bridge
#
# RGD: krotest07a-cross-rgd-producer-rg.yaml  |  Kind: KroTest07Producer
#
# !! REAL AWS RESOURCES — INCURS COST if using non-free-tier account !!
# Creates: VPC, IGW, SecurityGroup — ~$0/mo (no NAT GW)
#
# PREREQUISITES:
#   - ACK controller 'ec2' must be running
#   - Deploy this BEFORE Test 07b (wave 15 < consumer wave 20)
#   - Namespace must have services.k8s.aws/owner-account-id annotation
#     (created by kind-local-test.sh for kro-test-crossrgd namespace)
#
# Uncomment the block below to deploy.
# ----------------------------------------------------------------------------
# apiVersion: kro.run/v1alpha1
# kind: KroTest07Producer
# metadata:
#   name: kro-crossrgd-producer
#   namespace: kro-test-crossrgd
#   annotations:
#     argocd.argoproj.io/sync-wave: "15"
#     argocd.argoproj.io/sync-options: SkipDryRunOnMissingResource=true,ServerSideApply=true,Prune=false
# spec:
#   name: crossrgd-prod
#   namespace: kro-test-crossrgd
#   environment: local
#   region: us-east-1
#   adoptionPolicy: adopt-or-create
#   deletionPolicy: delete
#   project: gen3-kro
#   vpcCidr: "10.102.0.0/16"
