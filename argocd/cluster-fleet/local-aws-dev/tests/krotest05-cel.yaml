# ── Test 05: CEL expressions — KroCELTest ────────────────────────────────────
# Validates: ternary, string ops (split/join/slice), math, optionals.
# Pure K8s only — zero AWS cost. Creates ConfigMaps.
#
# RGD: krotest05-cel-expressions-rg.yaml  |  Kind: KroCELTest
#
# Uncomment ONE or BOTH blocks below to deploy.
# ----------------------------------------------------------------------------

# ── Variant: dev (minimal config) ────────────────────────────────────────────
# apiVersion: kro.run/v1alpha1
# kind: KroCELTest
# metadata:
#   name: kro-cel-dev
#   namespace: kro-test-cel
#   annotations:
#     argocd.argoproj.io/sync-wave: "15"
#     argocd.argoproj.io/sync-options: SkipDryRunOnMissingResource=true,ServerSideApply=true,Prune=false
# spec:
#   name: cel-dev
#   namespace: kro-test-cel
#   environment: local
#   fullDNSName: cel-dev.local.example.com
#   tagList: "env:local,team:platform,project:gen3-kro"
#   tier: dev
#   replicas: 1
#   optionalLabel: ""

---
# ── Variant: prod (higher replicas, prod tier) ───────────────────────────────
# apiVersion: kro.run/v1alpha1
# kind: KroCELTest
# metadata:
#   name: kro-cel-prod
#   namespace: kro-test-cel-prod
#   annotations:
#     argocd.argoproj.io/sync-wave: "15"
#     argocd.argoproj.io/sync-options: SkipDryRunOnMissingResource=true,ServerSideApply=true,Prune=false
# spec:
#   name: cel-prod
#   namespace: kro-test-cel-prod
#   environment: local
#   fullDNSName: cel-prod.local.example.com
#   tagList: "env:local,team:platform,project:gen3-kro,tier:prod"
#   tier: prod
#   replicas: 3
#   optionalLabel: critical
