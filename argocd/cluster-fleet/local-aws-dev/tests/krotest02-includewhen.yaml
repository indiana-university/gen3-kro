# ── Test 02: includeWhen — KroIncludeWhenTest ─────────────────────────────────
# Validates: single-condition, AND multi-condition, OR, cascade includeWhen.
# Pure K8s only — zero AWS cost. Creates ConfigMaps.
#
# RGD: krotest02-includewhen-rg.yaml  |  Kind: KroIncludeWhenTest
#
# Uncomment ONE or BOTH blocks below to deploy.
# ----------------------------------------------------------------------------

# ── Variant: minimal (featureA only) ────────────────────────────────────────
# apiVersion: kro.run/v1alpha1
# kind: KroIncludeWhenTest
# metadata:
#   name: kro-includewhen-minimal
#   namespace: kro-test-includewhen
#   annotations:
#     argocd.argoproj.io/sync-wave: "15"
#     argocd.argoproj.io/sync-options: SkipDryRunOnMissingResource=true,ServerSideApply=true,Prune=false
# spec:
#   name: includewhen-minimal
#   namespace: kro-test-includewhen
#   environment: local
#   featureA: true
#   featureB: false
#   featureC: false
#   tier: dev

---
# ── Variant: full (all features + prod tier) ──────────────────────────────────
# apiVersion: kro.run/v1alpha1
# kind: KroIncludeWhenTest
# metadata:
#   name: kro-includewhen-full
#   namespace: kro-test-includewhen-full
#   annotations:
#     argocd.argoproj.io/sync-wave: "15"
#     argocd.argoproj.io/sync-options: SkipDryRunOnMissingResource=true,ServerSideApply=true,Prune=false
# spec:
#   name: includewhen-full
#   namespace: kro-test-includewhen-full
#   environment: local
#   featureA: true
#   featureB: true
#   featureC: true
#   tier: prod
