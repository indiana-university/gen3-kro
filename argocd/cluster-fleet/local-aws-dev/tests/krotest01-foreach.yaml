# ── Test 01: forEach — KroForEachTest ────────────────────────────────────────
# Validates: single-dimension forEach, cartesian product forEach.
# Pure K8s only — zero AWS cost. Creates ConfigMaps.
#
# RGD: krotest01-foreach-rg.yaml  |  Kind: KroForEachTest
#
# Uncomment ONE or BOTH blocks below to deploy.
# ----------------------------------------------------------------------------

# ── Variant: basic (single dimension) ───────────────────────────────────────
# apiVersion: kro.run/v1alpha1
# kind: KroForEachTest
# metadata:
#   name: kro-foreach-basic
#   namespace: kro-test-foreach
#   annotations:
#     argocd.argoproj.io/sync-wave: "15"
#     argocd.argoproj.io/sync-options: SkipDryRunOnMissingResource=true,ServerSideApply=true,Prune=false
# spec:
#   name: foreach-basic
#   namespace: kro-test-foreach
#   environment: local
#   values:
#     - alpha
#     - beta
#     - gamma
#   regions:
#     - us-east-1
#   tiers:
#     - web
#   cartesianEnabled: false

---
# ── Variant: cartesian (multi-dimension product) ─────────────────────────────
# apiVersion: kro.run/v1alpha1
# kind: KroForEachTest
# metadata:
#   name: kro-foreach-cartesian
#   namespace: kro-test-foreach-cart
#   annotations:
#     argocd.argoproj.io/sync-wave: "15"
#     argocd.argoproj.io/sync-options: SkipDryRunOnMissingResource=true,ServerSideApply=true,Prune=false
# spec:
#   name: foreach-cartesian
#   namespace: kro-test-foreach-cart
#   environment: local
#   values:
#     - alpha
#     - beta
#   regions:
#     - us-east-1
#     - us-west-2
#   tiers:
#     - web
#     - api
#   cartesianEnabled: true
