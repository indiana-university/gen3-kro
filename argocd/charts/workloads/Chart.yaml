# Workloads chart — renders ArgoCD Applications per workload entry.
# Each enabled workload becomes an ArgoCD Application pointing at the spoke cluster.
# Values are sourced from cluster-fleet/<spoke>/workload.yaml via the fleet ApplicationSet.
apiVersion: v2
name: workloads
description: Application workloads deployed to spoke clusters via ArgoCD Applications
type: application
version: 0.2.0
appVersion: "1.0.0"

maintainers:
  - name: RDServices
    email: rdservices@iu.edu
  - name: Basanami (Jimi) Adeyemi
    email: boadeyem@iu.edu

keywords:
  - gen3
  - workloads
  - argocd
  - spoke
