# AwsGen3Dns1 — Tier 0.5: DNS + TLS Certificate (Modular RGD v1)
#
# Manages a Route53 hosted zone via ACK resource adoption and creates
# an ACM certificate plus DNS validation record for a spoke hostname.
# Produces a single dnsBridge ConfigMap consumed by Helm1 (acmBridgeName).
#
# Hosted zone adoption uses the ACK adoption-fields annotation with the hosted
# zone name. The instance no longer provides a Route53 hosted zone ID.
# ACK adopts the zone when it already exists and will create it if the
# adopt-or-create policy is used and the zone does not exist.
#
# Hosted zone path (application hostname is non-empty):
#   hostedZone (ACK Route53 adopt-or-create) → acmCert (ACM) → dnsValidation (Route53 RecordSet)
#   └── dnsBridge ConfigMap: {hosted-zone-id, hosted-zone-name, acm-cert-arn, hostname}
#
# Placeholder path (application hostname is empty):
#   dnsBridge ConfigMap: all keys empty
#
# Downstream consumer: Helm1 reads dnsBridge via acmBridgeName instance spec field.
# Bridge keys: hosted-zone-id, hosted-zone-name, acm-cert-arn, hostname

apiVersion: kro.run/v1alpha1
kind: ResourceGraphDefinition
metadata:
  name: awsgen3dns1
  annotations:
    argocd.argoproj.io/sync-wave: "0"
spec:
  schema:
    apiVersion: v1alpha1
    kind: AwsGen3Dns1
    spec:
      # ── Identity ──────────────────────────────────────────────────
      name: string | required=true
      namespace: string | required=true
      adoptionPolicy: string | default="adopt-or-create"
      deletionPolicy: string | default="delete"
      infrastructureConfigMapName: string | default="infrastructure-values"

      # ── Bridge ──────────────────────────────────────────
      dnsBridgeName: string | default="dns-bridge"

    status:
      hostedZoneID: "${infrastructureConfig.data['hostname'] != '' ? hostedZone.status.?id.orValue('loading') : ''}"
      acmCertificateARN: "${infrastructureConfig.data['hostname'] != '' ? acmCert.status.?ackResourceMetadata.?arn.orValue('loading') : ''}"

  resources:
  # ######################################################################
  # Hosted Zone + ACM Certificate + DNS Validation
  # All resources share the same includeWhen (Test 8 co-inclusion pattern).
  # ######################################################################

    - id: hostedZone
      includeWhen:
        - ${infrastructureConfig.data['hostname'] != ''}
      readyWhen:
        - ${hostedZone.status.?id.orValue('null') != 'null'}
        - ${hostedZone.status.?conditions.orValue([]).exists(c, c.type == "ACK.ResourceSynced" && c.status == "True")}
      template:
        apiVersion: route53.services.k8s.aws/v1alpha1
        kind: HostedZone
        metadata:
          name: ${schema.spec.name}-hosted-zone
          namespace: ${schema.spec.namespace}
          annotations:
            services.k8s.aws/region: ${infrastructureConfig.data['aws-region']}
            services.k8s.aws/adoption-policy: adopt
            # ACK Route53 HostedZone adoption requires "id" (the zone ID path),
            # NOT "name". Using "name" causes: error="required field missing: id"
            # on every reconcile loop. Confirmed by test krotest-route53-adoption.
            services.k8s.aws/adoption-fields: |
              {"id":"${infrastructureConfig.data['dns-hosted-zone-id']}"}
            services.k8s.aws/deletion-policy: retain
        spec:
          name: ${infrastructureConfig.data['dns-hosted-zone-name']}

    - id: infrastructureConfig
      readyWhen:
        - ${infrastructureConfig.metadata.?name.orValue('null') != 'null'}
      externalRef:
        apiVersion: v1
        kind: ConfigMap
        metadata:
          name: ${schema.spec.infrastructureConfigMapName}
          namespace: ${schema.spec.namespace}

  # ── ACM Certificate ───────────────────────────────────────────────────
    - id: acmCert
      includeWhen:
        - ${infrastructureConfig.data['hostname'] != ''}
      readyWhen:
        - ${acmCert.status.?ackResourceMetadata.?arn.orValue('null') != 'null'}
        - ${acmCert.status.?conditions.orValue([]).exists(c, c.type == "ACK.ResourceSynced" && c.status == "True")}
        - ${acmCert.status.?domainValidations.orValue([]).size() > 0}
      template:
        apiVersion: acm.services.k8s.aws/v1alpha1
        kind: Certificate
        metadata:
          name: ${schema.spec.name}-acm-cert
          namespace: ${schema.spec.namespace}
          annotations:
            services.k8s.aws/region: ${infrastructureConfig.data['aws-region']}
            services.k8s.aws/adoption-policy: ${schema.spec.adoptionPolicy}
            services.k8s.aws/deletion-policy: ${schema.spec.deletionPolicy}
        spec:
          domainName: ${infrastructureConfig.data['hostname']}
          tags:
            - key: Name
              value: ${schema.spec.name}-acm-cert
            - key: Environment
              value: ${infrastructureConfig.data['aws-environment']}
            - key: ManagedBy
              value: Kro
            - key: Project
              value: ${infrastructureConfig.data['aws-project']}
            - key: HostedZoneId
              value: ${hostedZone.status.?id.orValue("loading")}

  # ── DNS Validation RecordSet ──────────────────────────────────────
    - id: dnsValidation
      includeWhen:
        - ${infrastructureConfig.data['hostname'] != ''}
      readyWhen:
        - ${dnsValidation.status.?status.orValue('null') != 'null'}
        - ${dnsValidation.status.?conditions.orValue([]).exists(c, c.type == "ACK.ResourceSynced" && c.status == "True")}
      template:
        apiVersion: route53.services.k8s.aws/v1alpha1
        kind: RecordSet
        metadata:
          name: ${schema.spec.name}-acm-validation
          namespace: ${schema.spec.namespace}
          annotations:
            services.k8s.aws/region: ${infrastructureConfig.data['aws-region']}
            services.k8s.aws/adoption-policy: ${schema.spec.adoptionPolicy}
            services.k8s.aws/deletion-policy: ${schema.spec.deletionPolicy}
        spec:
          hostedZoneID: ${hostedZone.status.?id.orValue("loading")}
          name: ${acmCert.status.domainValidations[0].resourceRecord.name}
          recordType: CNAME
          ttl: 300
          resourceRecords:
            - value: ${acmCert.status.domainValidations[0].resourceRecord.value}

  # ── DNS Bridge ────────────────────────────────────────────────────
  # DUAL-BRIDGE PATTERN (Test 8): The bridge references hostedZone and acmCert
  # which are conditional (includeWhen: hostname != ''). If hostname is empty,
  # both nodes are excluded and KRO silently drops any resource referencing them.
  # Two opposite-includeWhen resources write to the same ConfigMap name so the
  # bridge always exists regardless of hostname configuration.
    - id: dnsBridgeWithHostname
      includeWhen:
        - ${infrastructureConfig.data['hostname'] != ''}
      readyWhen:
        - ${dnsBridgeWithHostname.metadata.?name.orValue('null') != 'null'}
      template:
        apiVersion: v1
        kind: ConfigMap
        metadata:
          name: ${schema.spec.dnsBridgeName}
          namespace: ${schema.spec.namespace}
          labels:
            app.kubernetes.io/managed-by: kro
            gen3.io/tier: dns
            gen3.io/bridge: "true"
            gen3.io/purpose: dns
        data:
          hosted-zone-id: ${hostedZone.status.?id.orValue('loading')}
          hosted-zone-name: ${infrastructureConfig.data['dns-hosted-zone-name']}
          acm-cert-arn: ${acmCert.status.?ackResourceMetadata.?arn.orValue('loading')}
          hostname: ${infrastructureConfig.data['hostname']}

    - id: dnsBridgeNoHostname
      includeWhen:
        - ${infrastructureConfig.data['hostname'] == ''}
      readyWhen:
        - ${dnsBridgeNoHostname.metadata.?name.orValue('null') != 'null'}
      template:
        apiVersion: v1
        kind: ConfigMap
        metadata:
          name: ${schema.spec.dnsBridgeName}
          namespace: ${schema.spec.namespace}
          labels:
            app.kubernetes.io/managed-by: kro
            gen3.io/tier: dns
            gen3.io/bridge: "true"
            gen3.io/purpose: dns
        data:
          hosted-zone-id: ""
          hosted-zone-name: ""
          acm-cert-arn: ""
          hostname: ""
