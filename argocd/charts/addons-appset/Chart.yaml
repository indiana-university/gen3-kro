apiVersion: v2
name: addon-appset
description: "Templated ApplicationSet deployer for addons; reads catalog, enablement and values via repo paths"
type: application
version: 0.1.0
appVersion: "0.1.0"
