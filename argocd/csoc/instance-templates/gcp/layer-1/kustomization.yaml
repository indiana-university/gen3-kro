apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Base GCP infrastructure instances
# These serve as templates for spoke-specific overlays

resources:

  - namespace.yaml
  # - vpc-instance.yaml
  # - gke-instance.yaml
  # - cloudsql-instance.yaml
  # - storage-bucket-instance.yaml
