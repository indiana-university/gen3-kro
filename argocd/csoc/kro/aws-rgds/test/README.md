# KRO Capability Tests

These ResourceGraphDefinitions are inactive by default. The `csoc-kro`
ApplicationSet excludes this directory so production sync only installs the Gen3
RGDs.

Apply individual files manually when testing KRO behavior. Tests 6 and 7 create
real ACK EC2 resources if instances are created from their CRDs.
