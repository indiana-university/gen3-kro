# AwsGen3AuditG3AutoSecret1 — AWS SM metadata + audit-g3auto value bootstrap
apiVersion: kro.run/v1alpha1
kind: ResourceGraphDefinition
metadata:
  name: awsgen3auditg3autosecret1
  annotations:
    argocd.argoproj.io/sync-wave: "35"
spec:
  schema:
    apiVersion: v1alpha1
    kind: AwsGen3AuditG3AutoSecret1
    spec:
      name: string | required=true
      namespace: string | required=true
      infrastructureConfigMapName: string | default="infrastructure-values"
      adoptionPolicy: string | default="adopt-or-create"
      deletionPolicy: string | default="retain"
      secretBridgeName: string | default="audit-g3auto-secret-bridge"
      secretWriterImage: string | default="quay.io/cdis/awshelper:master"
      messagingConfigMapName: string | default="messaging-bridge"

    status:
      secretARN: ${awsSecret.status.?ackResourceMetadata.?arn.orValue("loading")}
      created: "${secretValueJob.status.?succeeded.orValue(0) == 1 ? 'true' : 'false'}"

  resources:

    - id: infrastructureConfig
      readyWhen:
        - ${infrastructureConfig.metadata.?name.orValue('null') != 'null'}
      externalRef:
        apiVersion: v1
        kind: ConfigMap
        metadata:
          name: ${schema.spec.infrastructureConfigMapName}
          namespace: ${schema.spec.namespace}

    - id: spokeNamespace
      readyWhen:
        - ${spokeNamespace.metadata.?name.orValue('null') != 'null'}
        - ${spokeNamespace.metadata.annotations['services.k8s.aws/owner-account-role-arn'] != ''}
      externalRef:
        apiVersion: v1
        kind: Namespace
        metadata:
          name: ${schema.spec.namespace}

    - id: secretWriterServiceAccount
      readyWhen:
        - ${secretWriterServiceAccount.metadata.?name.orValue('null') != 'null'}
      externalRef:
        apiVersion: v1
        kind: ServiceAccount
        metadata:
          name: gen3-secret-writer-sa
          namespace: ${schema.spec.namespace}

    - id: messagingConfig
      readyWhen:
        - ${messagingConfig.metadata.?name.orValue('null') != 'null'}
      externalRef:
        apiVersion: v1
        kind: ConfigMap
        metadata:
          name: ${schema.spec.messagingConfigMapName}
          namespace: ${schema.spec.namespace}

    - id: awsSecret
      readyWhen:
        - ${awsSecret.status.?ackResourceMetadata.?arn.orValue('null') != 'null'}
        - ${awsSecret.status.?conditions.orValue([]).exists(c, c.type == "ACK.ResourceSynced" && c.status == "True")}
      template:
        apiVersion: secretsmanager.services.k8s.aws/v1alpha1
        kind: Secret
        metadata:
          name: ${schema.spec.name}-audit-g3auto-sm
          namespace: ${schema.spec.namespace}
          annotations:
            services.k8s.aws/region: ${infrastructureConfig.data['aws-region']}
            services.k8s.aws/adoption-policy: ${schema.spec.adoptionPolicy}
            services.k8s.aws/deletion-policy: ${schema.spec.deletionPolicy}
        spec:
          name: "${schema.spec.name + '-' + schema.spec.namespace + '-' + (infrastructureConfig.data['secret-name-audit-g3auto'] != '' ? infrastructureConfig.data['secret-name-audit-g3auto'] : 'audit-g3auto')}"
          description: ${"Audit g3auto config for " + schema.spec.name + " in " + schema.spec.namespace}
          tags:
            - key: ManagedBy
              value: Kro
            - key: Gen3Instance
              value: ${schema.spec.name}
            - key: Gen3Namespace
              value: ${schema.spec.namespace}
            - key: SecretPurpose
              value: audit-g3auto

    - id: secretValueJob
      readyWhen:
        - ${secretValueJob.status.?succeeded.orValue(0) == 1}
      template:
        apiVersion: batch/v1
        kind: Job
        metadata:
          name: ${schema.spec.name}-audit-g3auto-sync
          namespace: ${schema.spec.namespace}
          labels:
            app.kubernetes.io/managed-by: kro
            gen3.io/tier: secret-bootstrap
            gen3.io/secret-purpose: audit-g3auto
        spec:
          backoffLimit: 3
          template:
            spec:
              serviceAccountName: ${secretWriterServiceAccount.metadata.name}
              restartPolicy: Never
              containers:
                - name: sync-secret-value
                  image: ${schema.spec.secretWriterImage}
                  imagePullPolicy: IfNotPresent
                  env:
                    - name: AWS_REGION
                      value: ${infrastructureConfig.data['aws-region']}
                    - name: SECRET_ARN
                      value: ${awsSecret.status.ackResourceMetadata.arn}
                    - name: SPOKE_ROLE_ARN
                      value: ${spokeNamespace.metadata.annotations['services.k8s.aws/owner-account-role-arn']}
                    - name: AUDIT_SQS_URL
                      value: ${messagingConfig.data['audit-sqs-url']}
                  command:
                    - /bin/bash
                    - -c
                  args:
                    - |
                      set -euo pipefail

                      assume_spoke_role() {
                        local creds
                        creds="$(aws sts assume-role \
                          --role-arn "${SPOKE_ROLE_ARN}" \
                          --role-session-name "gen3-secret-bootstrap-${HOSTNAME:-job}" \
                          --query 'Credentials.[AccessKeyId,SecretAccessKey,SessionToken]' \
                          --output text)"
                        read -r AWS_ACCESS_KEY_ID AWS_SECRET_ACCESS_KEY AWS_SESSION_TOKEN <<< "$creds"
                        export AWS_ACCESS_KEY_ID AWS_SECRET_ACCESS_KEY AWS_SESSION_TOKEN
                      }

                      assume_spoke_role

                      existing="$(aws secretsmanager get-secret-value --region "$AWS_REGION" --secret-id "$SECRET_ARN" --query SecretString --output text 2>/dev/null || true)"
                      if [[ -n "$existing" && "$existing" != "None" ]]; then
                        echo "audit-g3auto already exists in AWS; leaving it unchanged."
                        exit 0
                      fi

                      audit_config="$(python3 - <<'PY2'
                      import os, json
                      print(json.dumps({
                          "sqs_url": os.environ['AUDIT_SQS_URL'],
                          "aws_region": os.environ['AWS_REGION'],
                      }))
                      PY2
                      )"
                      aws secretsmanager put-secret-value --region "$AWS_REGION" --secret-id "$SECRET_ARN" --secret-string "$audit_config"
                      echo "audit-g3auto secret value synced."

    - id: secretBridge
      readyWhen:
        - ${secretBridge.metadata.?name.orValue('null') != 'null'}
      template:
        apiVersion: v1
        kind: ConfigMap
        metadata:
          name: ${schema.spec.secretBridgeName}
          namespace: ${schema.spec.namespace}
          labels:
            app.kubernetes.io/managed-by: kro
            gen3.io/tier: secret-bootstrap
            gen3.io/bridge: "true"
        data:
          secret-name: "${schema.spec.name + '-' + schema.spec.namespace + '-' + (infrastructureConfig.data['secret-name-audit-g3auto'] != '' ? infrastructureConfig.data['secret-name-audit-g3auto'] : 'audit-g3auto')}"
          secret-arn: ${awsSecret.status.?ackResourceMetadata.?arn.orValue("loading")}
          created: "${secretValueJob.status.?succeeded.orValue(0) == 1 ? 'true' : 'false'}"
