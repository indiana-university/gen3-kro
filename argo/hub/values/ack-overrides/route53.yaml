# Override values for route53 ACK controller

serviceAccount:
  annotations:
    eks.amazonaws.com/role-arn: "{{.Values.global.roleArns.route53}}"

resources:
  limits:
    cpu: 200m
    memory: 256Mi
  requests:
    cpu: 100m
    memory: 128Mi
