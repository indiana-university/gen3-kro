apiVersion: v2
name: ack-controllers
description: ACK (AWS Controllers for Kubernetes) Controllers
type: application
version: 1.0.0
appVersion: "1.0"

# NOTE: ACK Helm charts deployment disabled - use individual controller deployments instead
# The OCI chart format requires specific versioning that conflicts with dynamic deployment
# Individual controllers can be deployed directly to ack-system namespace
dependencies: []
